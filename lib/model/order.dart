// ignore_for_file: non_constant_identifier_names

class Order {
  String total, order_id, bill_id, dateorder;

  Order(
      {required this.total,
      required this.order_id,
      required this.bill_id,
      required this.dateorder,
      });
}